---
"nanoclaw-webchat": minor
---

Add `WEBCHAT_PUBLIC_PATH` so reverse-proxy path mounts (e.g. `/webchat` with stripPrefix) rewrite `/api` and `/assets` in served SPA assets and post-login redirects.
