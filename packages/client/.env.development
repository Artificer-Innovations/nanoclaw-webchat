# Default NanoClaw host for direct WebSocket in dev (REST still uses the Vite proxy).
# Override in .env.development.local if your host runs on a different port or host.
VITE_WEBCHAT_API_TARGET=http://127.0.0.1:3200
