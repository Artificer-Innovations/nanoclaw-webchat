---
description: Require typecheck, unit tests, and coverage to pass before pushing
alwaysApply: true
---

# Pre-push verification

Before committing or pushing changes, run these commands locally and confirm they all pass:

```bash
pnpm run typecheck
pnpm run test:unit
pnpm run test:coverage
```

Do not push if any command fails. Fix failures first, then re-run the full sequence.

`test:coverage` runs each package with its own Vitest thresholds:

- **client, cli, mcp**: 100% (statements, branches, functions, lines)
- **adapter** (host fixture at `test/fixtures/nanoclaw-host/vitest.config.ts`): 98%

Treat any coverage regression as a blocker for the package that failed.

When tests involve WebSocket mocks or fake timers, prefer waiting for async side effects (e.g. `waitFor`) rather than assuming synchronous setup after render.
